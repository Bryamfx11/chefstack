# Plantilla de secretos LOCALES. Copia este archivo a "secrets.properties" y completa el valor.
# El archivo real (secrets.properties) esta en .gitignore y NUNCA se sube al repositorio.

# API key de Spoonacular (https://spoonacular.com/food-api -> Profile)
spoonacular.api-key=TU_API_KEY_DE_SPOONACULAR
